/*>>>Variable type double is used to store numbers like 1.286(having long extensions after decimal point, float less and doble long extensions)
>>>To determine male or female,true or false,we use boolean variable
>>>Data types are of 3 types:-Built-in(int,float,char,double,bool),User-defined(struct,union,enum) and derived(array,function,pointers)
>>>Certain rules to be followed before writing characters(CHECK THE SOURCE CODE FOR THE RULES*/

#include <iostream>
int glo = 6;
void sum()
{
    int a;
   
}
using namespace std;
int main()
{
    int glo = 9;
    glo = 79;
    int a = 4;
    int b = 5;
    float pi = 3.14;
    char c = 't'; //Only one character allowed
    bool is_true = true;//If written true then 1 and false then 0
    sum();
  cout<<glo<<is_true;
    

    cout << "\nHere the value of a is " << a << ".\nThe value of b is " << b;
    cout << ".\nThe value of pi is " << pi;
    cout << ".\nThe entered character is " << c;
    cout << "\n"
         << glo;
    return 0;
}
